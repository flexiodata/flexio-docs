name: Delete

method_name:
  javascript: delete

description: |
  Deletes the specified file.

returns: |

rest_parameters:
  - property: '`path`'
    type: string
    description: The path of the file to delete.

json_str: |
  {
    "op": "delete",
    "path": "/my-cloud-storage/file.txt"
  }
