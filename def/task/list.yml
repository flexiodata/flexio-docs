name: List

method_name:
  javascript: list

description: |
  Lists the contents of a directory.

returns: |
  Returns the contents of the specified directory.

rest_parameters:
  - property: '`path`'
    type: string
    description: |
      The path of the directory contents to list.

json_str: |
  {
    "op": "list",
    "path": ""
  }
